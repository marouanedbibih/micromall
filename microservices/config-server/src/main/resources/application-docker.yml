spring:
  config:
    activate:
      on-profiles:
        - docker
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marouanedbibih/micromall
          default-label: main
          search-paths: configurations
